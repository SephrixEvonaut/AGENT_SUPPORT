# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
# SWTOR GESTURE MANIFEST
# Profile: SWTOR Vengeance Juggernaut - Full Rotation
# Generated: 2026-01-06
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#
# LEGEND
# ───────────────────────────────────────────────────────────────────────────────────────────────────────
# BUFFER TIERS:     ▸ low (25-40ms)  ▸ medium (80-120ms)  ▸ high (180-250ms)
# ICONS:            🎯 cog = NUMPAD_SUBTRACT   🔫 gun = NUMPAD_ADD   🛡️ shield = NUMPAD_MULTIPLY (dualKey)
# DUAL KEY:         [key₁ +6ms→ key₂] = Two keys pressed near-simultaneously
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

profile:
  name: "SWTOR Vengeance Juggernaut - Full Rotation"
  description: "Complete keybind profile with focus modifier, targeting, conundrum-optimized assignment, Endure Pain, Enraged Defense, Discord controls, and extended timer system"

gestureSettings:
  multiPressWindow: 80
  debounceDelay: 10
  longPressMin: 80
  longPressMax: 145
  superLongMin: 146
  superLongMax: 265
  cancelThreshold: 266

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: W
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"W":
  single:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_long:
    name: "Mad Dash"
    sequence:
      - { key: "ALT+Q", buffer: low }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: "Jump"
    sequence:
      - { key: "NUMPAD0", buffer: low }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: "Jump + Acquire Center Target + Leap"
    sequence:
      - { key: "NUMPAD0", buffer: low }
      - { key: "SHIFT+O", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "F9", buffer: low }

  triple:
    name: "Jump + Acquire Center Target"
    sequence:
      - { key: "NUMPAD0", buffer: low }
      - { key: "SHIFT+O", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: "Leap Spam (4x)"
    sequence:
      - { key: "F9", buffer: low }
      - { key: "F9", buffer: low }
      - { key: "F9", buffer: low }
      - { key: "F9", buffer: low }

  quadruple_long:
    name: "Jump + Acquire Center Target + Target of Target + Intercede"
    sequence:
      - { key: "NUMPAD0", buffer: low }
      - { key: "SHIFT+O", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: ";", buffer: low }

  quadruple_super_long:
    name: "Jump + Acquire Center Target + Target of Target + Guard Swap"
    sequence:
      - { key: "NUMPAD0", buffer: low }
      - { key: "SHIFT+O", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: A
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"A":
  single:
    name: "Leap"
    sequence:
      - { key: "F9", buffer: low }

  single_long:
    name: "Single Target Taunt"
    sequence:
      - { key: "F6", buffer: low }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: "Single Target Taunt (Check for Threatening Rage)"
    sequence:
      - { key: "F6", buffer: low }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: S
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"S":
  single:
    name: "Group Member 1"
    sequence:
      - { key: "NUMPAD1", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  single_long:
    name: "Group Member 2"
    sequence:
      - { key: "NUMPAD2", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: "Group Member 3"
    sequence:
      - { key: "NUMPAD3", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: "Target of Target"
    sequence:
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: "Mass Taunt + Focus Modifier + Single Target Taunt + Enrage (1.2s delay)"
    sequence:
      - { key: "F7", buffer: low }
      - { key: "X", buffer: medium }
      - { key: "NUMPAD_ADD", buffer: low }
      - { key: "F6", buffer: high }
      - { key: "F8", delay: "1200-1210" }

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: D
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"D":
  single:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_long:
    name: "Escape Key"
    sequence:
      - { key: "ESCAPE", buffer: low }

  single_super_long:
    name: "Timer: Burst (13s) - TTS: 'burst'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'burst' after 13 seconds" }

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: B
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"B":
  single:
    name: "Out of Combat Heal"
    sequence:
      - { key: "NUMPAD_DECIMAL", buffer: low }

  single_long:
    name: "RP Walk Toggle"
    sequence:
      - { key: "BACKSPACE", buffer: low }

  single_super_long:
    name: "Mute SWTOR"
    sequence:
      - { key: "END", buffer: low }

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: "Mute SWTOR"
    sequence:
      - { key: "END", buffer: low }

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: I
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"I":
  single:
    name: "Focus Target Crushing Blow"
    sequence:
      - { key: "SHIFT+R", buffer: low, dualKey: "N", dualKeyOffset: 6 }

  single_long:
    name: "Focus Target Interrupt"
    sequence:
      - { key: "SHIFT+R", buffer: low, dualKey: "K", dualKeyOffset: 6 }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: "Focus Target Force Scream"
    sequence:
      - { key: "SHIFT+R", buffer: low, dualKey: "O", dualKeyOffset: 6 }

  double_long:
    name: "Focus Target Single Taunt"
    sequence:
      - { key: "SHIFT+R", buffer: low, dualKey: "F6", dualKeyOffset: 6 }

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: "Focus Target Vicious Throw"
    sequence:
      - { key: "SHIFT+R", buffer: low, dualKey: "[", dualKeyOffset: 6 }

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: Y
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"Y":
  single:
    name: "Saber Ward"
    sequence:
      - { key: ",", buffer: low }

  single_long:
    name: "Invincible"
    sequence:
      - { key: "ALT+M", buffer: low }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: "Enraged Defense + Single Target Taunt"
    sequence:
      - { key: "SHIFT+.", buffer: low }
      - { key: "F6", buffer: low }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: U
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"U":
  single:
    name: "Med Pack"
    sequence:
      - { key: "ALT+O", buffer: low }

  single_long:
    name: "Adrenal"
    sequence:
      - { key: "ALT+N", buffer: low }

  single_super_long:
    name: "Relic"
    sequence:
      - { key: "SHIFT+X", buffer: low }

  double:
    name: "Endure Pain + Med Pack"
    sequence:
      - { key: "SHIFT+,", buffer: low }
      - { key: "ALT+O", buffer: low }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: T
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"T":
  single:
    name: "Timer: Drop (16s) - TTS: 'drop inc'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'drop inc' after 16 seconds" }

  single_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: C
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"C":
  single:
    name: "Timer: Burst (13s) - TTS: 'burst'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'burst' after 13 seconds" }

  single_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: H
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"H":
  single:
    name: "Timer: Laze (31s) - TTS: 'laze'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'laze' after 31 seconds" }

  single_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: P
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"P":
  single:
    name: "Timer: Burst (13s) - TTS: 'burst'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'burst' after 13 seconds" }

  single_long:
    name: "Timer: Yield (45s) - TTS: 'yield'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'yield' after 45 seconds" }

  single_super_long:
    name: "Timer: Fuel (103s) - TTS: 'fuel'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'fuel' after 1 minute 43 seconds" }

  double:
    name: "Timer: Laze (31s) - TTS: 'laze'"
    sequence:
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'laze' after 31 seconds" }

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 1
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"1":
  single:
    name: "Crushing Blow"
    sequence:
      - { key: "N", buffer: low }

  single_long:
    name: "Backhand"
    sequence:
      - { key: "ALT+R", buffer: low }

  single_super_long:
    name: "Force Choke"
    sequence:
      - { key: "SHIFT+Z", buffer: low }

  double:
    name: "Force Scream"
    sequence:
      - { key: "O", buffer: low }

  double_long:
    name: "Single Target Taunt + Crushing Blow"
    sequence:
      - { key: "F6", buffer: low }
      - { key: "N", buffer: low }

  double_super_long:
    name: "Close Enemy + Crushing Blow"
    sequence:
      - { key: "Q", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "N", buffer: low }

  triple:
    name: "Next Friend (with Cog)"
    sequence:
      - { key: ".", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: "Adrenal"
    sequence:
      - { key: "ALT+N", buffer: low }

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: "Close Enemy + Crushing Blow + Retaliation + Previous Enemy"
    sequence:
      - { key: "Q", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "N", buffer: low }
      - { key: "R", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_long:
    name: "Close Enemy + Crushing Blow + Retaliation + Previous Enemy"
    sequence:
      - { key: "Q", buffer: low }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "N", buffer: medium }
      - { key: "R", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 2
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"2":
  single:
    name: "Guard Swap (with Shield Icon)"
    sequence:
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }

  single_long:
    name: "Electro Stun"
    sequence:
      - { key: "ALT+K", buffer: low }

  single_super_long:
    name: "Force Push"
    sequence:
      - { key: "ALT+L", buffer: low }

  double:
    name: "Close Enemy (with Cog)"
    sequence:
      - { key: "Q", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  double_long:
    name: "Smash"
    sequence:
      - { key: "]", buffer: low }

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: "Close Friend (with Cog)"
    sequence:
      - { key: "'", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: "Med Pack"
    sequence:
      - { key: "ALT+O", buffer: low }

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: "Enrage"
    sequence:
      - { key: "F8", buffer: low }

  quadruple_long:
    name: "Group Member 2 (with Cog)"
    sequence:
      - { key: "NUMPAD2", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 3
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"3":
  single:
    name: "Aegis Assault"
    sequence:
      - { key: "Z", buffer: low }

  single_long:
    name: "Awe/Reflect/Mad Dash"
    sequence:
      - { key: "ALT+Q", buffer: low }

  single_super_long:
    name: "Seismic Grenade"
    sequence:
      - { key: "ALT+J", buffer: low }

  double:
    name: "Retaliation + Aegis Assault"
    sequence:
      - { key: "R", buffer: low }
      - { key: "Z", buffer: low }

  double_long:
    name: "Single Target Taunt + Aegis Assault"
    sequence:
      - { key: "F6", buffer: low }
      - { key: "Z", buffer: low }

  double_super_long:
    name: "Close Enemy + Aegis Assault"
    sequence:
      - { key: "Q", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "Z", buffer: low }

  triple:
    name: "Endure Pain + Timer: Drop (16s)"
    sequence:
      - { key: "SHIFT+,", buffer: low }
      - { key: "END", buffer: low, name: "Timer placeholder - implement TTS: 'drop inc' after 16 seconds" }

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: "Leap"
    sequence:
      - { key: "F9", buffer: low }

  quadruple_long:
    name: "Group Member 3 (with Cog)"
    sequence:
      - { key: "NUMPAD3", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 4
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"4":
  single:
    name: "Focus Target's Target of Target"
    sequence:
      - { key: "J", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  single_long:
    name: "Basic Attack"
    sequence:
      - { key: "SHIFT+Q", buffer: low }

  single_super_long:
    name: "Intercede"
    sequence:
      - { key: ";", buffer: low }

  double:
    name: "Next Enemy + Interrupt"
    sequence:
      - { key: "V", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "K", buffer: low }

  double_long:
    name: "Target of Target + Guard Swap"
    sequence:
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }

  double_super_long:
    name: "Target of Target + Intercede"
    sequence:
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: ";", buffer: low }

  triple:
    name: "Next Enemy + Interrupt + Previous Enemy"
    sequence:
      - { key: "V", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "K", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: "Target of Target + Guard Swap + Previous Enemy"
    sequence:
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_super_long:
    name: "Target of Target + Intercede + Previous Enemy"
    sequence:
      - { key: "M", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: ";", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: "Group Member 4 (with Cog)"
    sequence:
      - { key: "NUMPAD7", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 5
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"5":
  single:
    name: "Vicious Throw"
    sequence:
      - { key: "[", buffer: low }

  single_long:
    name: "Stun Break"
    sequence:
      - { key: "SHIFT+V", buffer: low }

  single_super_long:
    name: "Vicious Slash"
    sequence:
      - { key: "SHIFT+L", buffer: low }

  double:
    name: "Next Enemy + Interrupt"
    sequence:
      - { key: "V", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "K", buffer: low }

  double_long:
    name: "Focus Target's Target of Target + Guard Swap"
    sequence:
      - { key: "J", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }

  double_super_long:
    name: "Focus Target's Target of Target + Intercede"
    sequence:
      - { key: "J", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: ";", buffer: low }

  triple:
    name: "Acquire Center Target + Interrupt + Previous Enemy"
    sequence:
      - { key: "SHIFT+O", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "K", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_long:
    name: "Focus Target's Target of Target + Guard Swap + Previous Enemy"
    sequence:
      - { key: "J", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: "L", buffer: low, dualKey: "NUMPAD_MULTIPLY", dualKeyOffset: 6 }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  triple_super_long:
    name: "Focus Target's Target of Target + Intercede + Previous Enemy"
    sequence:
      - { key: "J", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }
      - { key: ";", buffer: low }
      - { key: "SHIFT+N", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: "Target Self (with Cog)"
    sequence:
      - { key: "NUMPAD9", buffer: medium }
      - { key: "NUMPAD_SUBTRACT", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 6
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"6":
  single:
    name: "Sweeping Slash"
    sequence:
      - { key: "SHIFT+J", buffer: low }

  single_long:
    name: "Relic"
    sequence:
      - { key: "SHIFT+X", buffer: low }

  single_super_long:
    name: "Retaliation + Crushing Blow"
    sequence:
      - { key: "R", buffer: low }
      - { key: "N", buffer: low }

  double:
    name: "Sweeping Slash + Single Target Taunt"
    sequence:
      - { key: "SHIFT+J", buffer: low }
      - { key: "F6", buffer: low }

  double_long:
    name: "Ravage + Single Target Taunt"
    sequence:
      - { key: "SHIFT+K", buffer: low }
      - { key: "F6", buffer: low }

  double_super_long:
    name: "Basic Attack + Single Target Taunt"
    sequence:
      - { key: "SHIFT+Q", buffer: low }
      - { key: "F6", buffer: low }

  triple:
    name: "Sweeping Slash + Set Focus Target + Single Target Taunt"
    sequence:
      - { key: "SHIFT+J", buffer: low }
      - { key: "X", buffer: medium }
      - { key: "NUMPAD_ADD", buffer: low }
      - { key: "F6", buffer: low }

  triple_long:
    name: "Ravage + Set Focus Target + Single Target Taunt"
    sequence:
      - { key: "SHIFT+K", buffer: low }
      - { key: "X", buffer: medium }
      - { key: "NUMPAD_ADD", buffer: low }
      - { key: "F6", buffer: low }

  triple_super_long:
    name: "Basic Attack + Set Focus Target + Single Target Taunt"
    sequence:
      - { key: "SHIFT+Q", buffer: low }
      - { key: "X", buffer: medium }
      - { key: "NUMPAD_ADD", buffer: low }
      - { key: "F6", buffer: low }

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: "OOC Heal"
    sequence:
      - { key: "NUMPAD_DECIMAL", buffer: low }

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 7
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"7":
  single:
    name: "Discord Volume: Low"
    sequence:
      - { key: "END", buffer: low, name: "Discord Volume: Low" }

  single_long:
    name: "Discord Mic Toggle"
    sequence:
      - { key: "CTRL+SHIFT+M", buffer: low, name: "Discord Mic Toggle" }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 8
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"8":
  single:
    name: "Discord Volume: Medium"
    sequence:
      - { key: "END", buffer: low, name: "Discord Volume: Medium" }

  single_long:
    name: "Discord Deafen Toggle"
    sequence:
      - { key: "CTRL+SHIFT+D", buffer: low, name: "Discord Deafen" }

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: 9
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"9":
  single:
    name: "Discord Volume: High + Deafen"
    sequence:
      - { key: "END", buffer: low, name: "Discord Volume: High" }
      - { key: "CTRL+SHIFT+D", buffer: low, name: "Discord Deafen" }

  single_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
#  KEY: MIDDLE_CLICK
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════

"MIDDLE_CLICK":
  single:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  single_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  double_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  triple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

  quadruple_super_long:
    name: ~                                         # ── NOT ASSIGNED ──
    sequence: []

# ═══════════════════════════════════════════════════════════════════════════════════════════════════════
# END OF MANIFEST
# ═══════════════════════════════════════════════════════════════════════════════════════════════════════